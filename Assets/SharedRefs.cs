using UnityEngine;
using System.Collections;

public static class SharedRefs {
    // make it easy for other scripts to call ScoreTrackerScript
    public static ScoreTrackerScript scoreTrackerScript;
}
